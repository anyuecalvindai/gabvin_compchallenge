# Minimal stand-in for the parts of scipy the challenge files use
# (just scipy.constants), so bohr.py / particleinbox.py / units.py can keep
# their imports unchanged without shipping the full ~30 MB scipy wheel.
